<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | 评论自动审核设置
    |--------------------------------------------------------------------------
    |
    | 当设置为 true 时，新评论将自动标记为已审核，无需管理员手动审批。
    | 设置为 false 时，新评论需要管理员在后台审核后才能在前台展示。
    |
    */
    'auto_approve_comments' => env('AUTO_APPROVE_COMMENTS', true),

    /*
    |--------------------------------------------------------------------------
    | 每页文章数
    |--------------------------------------------------------------------------
    |
    | 博客列表页每页显示的文章数量。
    |
    */
    'posts_per_page' => (int) env('POSTS_PER_PAGE', 14),

    /*
    |--------------------------------------------------------------------------
    | 评论最大长度
    |--------------------------------------------------------------------------
    |
    | 单条评论的最大字符数限制。
    |
    */
    'comment_max_length' => (int) env('COMMENT_MAX_LENGTH', 1000),

];
